import { PROJECTS_SECTION_HEIGHT } from "../constants"

const Projects = () => {
  return (
    <section style={{ height: `${PROJECTS_SECTION_HEIGHT}px` }}>
      <h1 className="text-xl">Projects Section</h1>
    </section>
  )
}

export default Projects
